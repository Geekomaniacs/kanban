document.querySelector('.addItem').addEventListener('click', (e) => {
  const box = e.target.parentElement;
  const name = box.querySelector('.content').value;
  const date = box.querySelector('.date').value.toString();
  const owner = box.querySelector('.owner').textContent;
  // fetch() {
    
  // }
});
